{
  "name": "card-launch-news",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tracks newly launched Indian credit cards by diffing issuer sitemaps and enriching candidates with public announcement/community data.",
  "scripts": {
    "run": "node --env-file-if-exists=.env scripts/run.mjs",
    "run:force": "node --env-file-if-exists=.env scripts/run.mjs --force",
    "backfill-summaries": "node --env-file-if-exists=.env scripts/backfill-summaries.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
